# residual-comparision-dashboard

# Residual Comparison Dashboard (Streamlit)

A Streamlit app to upload two Excel workbooks (last month + this month) and compare Residual Master programs by Title, Terms, Optional Miles, and Normal Miles.

## Run locally
```bash
pip install -r requirements.txt
streamlit run Residual_Comparison_Dashboard.py
